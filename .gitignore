# 字节编译/优化后的/DLL文件
__pycache__/
*.py[codz]
*$py.class

# C 扩展模块
*.so

# 分发/打包
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#   通常这些文件是由 Python 脚本根据模板生成的
#   在 PyInstaller 构建可执行文件之前，以便将日期或其他信息注入其中。
*.manifest
*.spec

# 安装程序日志
pip-log.txt
pip-delete-this-directory.txt

# 单元测试/覆盖率报告
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py.cover
.hypothesis/
.pytest_cache/
cover/

# 翻译
*.mo
*.pot

# Django 相关：
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask 相关：
instance/
.webassets-cache

# Scrapy 相关：
.scrapy

# Sphinx 文档
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
# 对于某个库或软件包，你可能希望忽略这些文件，因为其代码
# 旨在多个环境中运行；否则，请将其纳入版本控制：
# .python-version

# pipenv
#   根据 pypa/pipenv#598 的建议，推荐将 Pipfile.lock 纳入版本控制。
#   不过，在协作场景下，如果存在平台特定的依赖项或依赖项
# 由于缺乏跨平台支持，pipenv 可能会安装无法正常运行的依赖项，或者根本无法安装
# 安装所有所需的依赖项。
# Pipfile.lock

# UV
# 与 Pipfile.lock 类似，通常建议将 uv.lock 纳入版本控制。
# 尤其对于二进制包而言，这更有利于确保可重复性，而且更加
# 通常被库忽略的部分。
# uv.lock

# poetry
# 与 Pipfile.lock 类似，通常建议将 poetry.lock 文件纳入版本控制。
# 尤其对于二进制包而言，这更有利于确保可重复性，而且更加
# 通常被库忽略的部分。
# https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
# poetry.lock
# poetry.toml

# pdm
# 与 Pipfile.lock 类似，通常建议将 pdm.lock 纳入版本控制。
# pdm 建议将项目级配置写入 pdm.toml 文件，但应排除 .pdm-python 目录。
#   https://pdm-project.org/en/latest/usage/project/#working-with-version-control
# pdm.lock
# pdm.toml
.pdm-python
.pdm-build/

# pixi
# 类似于Pipfile.lock，通常建议将pixi.lock也纳入版本控制。
# pixi.lock
#   Pixi 在 .pixi 目录中创建一个虚拟环境，就像 venv 模块在 .venv 目录中创建一样
#   建议不要将该目录纳入版本控制。
.pixi

# PEP 582；例如被 github.com/David-OConnor/pyflow 和 github.com/pdm-project/pdm 等项目使用
__pypackages__/

# Celery 相关内容
celerybeat-schedule
celerybeat.pid

# Redis
*.rdb
*.aof
*.pid

# RabbitMQ
mnesia/
rabbitmq/
rabbitmq-data/

# ActiveMQ
activemq-data/

# SageMath 解析后的文件
*.sage.py

# 环境
.env
.envrc
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder 项目设置
.spyderproject
.spyproject

# Rope项目设置
.ropeproject

# mkdocs文档
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre 类型检查器
.pyre/

# pytype 静态类型分析器
.pytype/

# Cython 调试符号
cython_debug/

# PyCharm
# JetBrains 专用模板维护在单独的 JetBrains.gitignore 文件中，该文件可
# 在 https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore 查看
#   并且可以将其添加到全局的.gitignore文件中，或合并到此文件中。若想更彻底地
#   采取另一种方案（不推荐），您可以取消注释以下内容，以忽略整个idea文件夹。
# .idea/

# Abstra
#   Abstra 是一个由人工智能驱动的流程自动化框架。
#   忽略包含用户凭据、本地状态和设置的目录。
# 详情请访问 https://abstra.io/docs
.abstra/

# Visual Studio Code
# Visual Studio Code 专用模板单独维护在一份 VisualStudioCode.gitignore 文件中
# 该文件可在 https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore 查阅
# 并且可以将其添加到全局的.gitignore文件中，或合并到此文件中。不过，如果您更倾向于这样做，
# 您也可以取消注释以下内容，以忽略整个vscode文件夹
# .vscode/
# 用于部分代码执行的临时文件
tempCodeRunnerFile.py

# Ruff 相关内容：
.ruff_cache/

# PyPI 配置文件
.pypirc

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# Streamlit
.streamlit/secrets.toml
# ==========================================
# 以下是针对 AMSS_NCKU 项目的额外补充
# ==========================================

# 1. 忽略 C/Fortran 编译产生的中间文件和可执行文件
# (防止把编译好的二进制程序传上去)
*.o
*.mod
*.exe
build/
bin/

# 2. 忽略大型数据文件 (防止仓库体积过大)
#（建议只保留脚本，数据通过网盘分享或 Git LFS 管理）
*.h5
*.dat
*.txt
!README.md 
!requirements.txt 

# 3. 忽略操作系统生成的垃圾文件
.DS_Store
Thumbs.db

# 4. 如果你想忽略编辑器的个人配置，请取消下面两行的注释
# .vscode/
# .idea/
